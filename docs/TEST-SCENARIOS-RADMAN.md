# سناریوهای آزمون جامع محیط استیجینگ و پیش از لانچ (`TEST-SCENARIOS-RADMAN.md`)

> **مجموعه آزمون‌های کیفیت‌سنجی و تضمین عملکرد (Staging & Pre-Launch QA Test Scenarios)**  
> *این سند حداقل ۲۰ سناریوی آزمون عملیاتی را جهت بررسی سلامت فروشگاه رادمان سیلور (`radman-silver-store`) در محیط استیجینگ و قبل از لانچ عمومی تدوین می‌کند.*

---

## 1. Customer Storefront & Checkout Scenarios (`سناریوهای مرور و خرید مشتری`)

- [ ] Pending : **Browse Product Page:** مشاهده صفحه محصول انگشتر نقره ۹۲۵؛ بررسی صحت نمایش عنوان سئو، وزن نقره به گرم، عیار ۹۲۵ استرلینگ، نوع نگین معدنی و قیمت به **تومان** با فرمت لوکس.
- [ ] Pending : **Add to Cart:** افزودن محصول به سبد خرید؛ بررسی کارکرد صحیح دکمه افزودن به سبد، نمایش مینی‌کارت و عدم بریدگی متون فارسی.
- [ ] Pending : **Checkout Success (Real Payment):** تکمیل فرم سفارش، انتخاب استان/شهر ایران، انتقال به درگاه زرین‌پال، انجام تراکنش موفق با کارت شتاب و بازگشت صحیح به صفحه رسید ووکامرس.
- [ ] Pending : **Failed Payment Handling:** انصراف از پرداخت یا وارد کردن رمز اشتباه در درگاه زرین‌پال؛ بررسی بازگشت صحیح کاربر به صفحه سبد خرید با نمایش پیام خطای مناسب و عدم کسر موجودی انبار.
- [ ] Pending : **Callback Verification:** تأیید امنیت Callback درگاه پرداخت؛ اطمینان از اینکه ووکامرس با دریافت رسید معتبر بانکی، وضعیت سفارش را به درستی به `On-Hold` (`در انتظار بررسی`) تغییر می‌دهد.

---

## 2. Telegram Human-in-the-Loop & Order Scenarios (`سناریوهای تلگرام و تأیید سفارش`)

- [ ] Pending : **Order Created in WooCommerce:** بررسی ثبت دقیق اطلاعات مشتری (نام، تلفن، آدرس و اقلام سفارش) در پنل مدیریت ووکامرس.
- [ ] Pending : **Telegram Notification Sent:** بررسی دریافت آنی پیامک هشدار سفارش جدید در گروه مدیریتی تلگرام (`@RadmanSilverStoreBot`) به همراه شناسه سفارش و مبلغ کل.
- [ ] Pending : **Owner Approval Path (`تأیید موجودی و ارسال`):** کلیک مالک روی دکمه `[تأیید موجودی و ارسال]` در تلگرام؛ تأیید تغییر وضعیت خودکار سفارش به `Processing` (`در حال پردازش`) و کسر موجودی کالا.
- [ ] Pending : **Owner Rejection Path (`عدم موجودی و لغو`):** کلیک مالک روی دکمه `[عدم موجودی و لغو]` در تلگرام؛ تأیید تغییر وضعیت سفارش به `Cancelled` (`لغو شده`) و صدور هشدار استرداد وجه.
- [ ] Pending : **Out-of-Stock Item Behavior:** بررسی رفتار سایت هنگام اتمام موجودی یک کالا (`stock = 0`)؛ تأیید تغییر دکمه به «ناموجود» و ممانعت از افزودن به سبد خرید.
- [ ] Pending : **`stock = 1` Sellable Behavior:** بررسی کالاهایی که موجودی آن‌ها دقیقاً `1` عدد است؛ تأیید امکان ثبت سفارش و فروش موفق (طبق قانون تک‌محصول بودن انگشترهای نقره).

---

## 3. Pricing Modes & Legacy Sync Scenarios (`سناریوهای قیمت‌گذاری و همگام‌سازی`)

- [ ] Pending : **`manual_locked` Product Price Unchanged:** اجرای دستور تلگرامی `/price 85000` توسط مالک؛ تأیید اینکه محصولات قفل‌شده دستی (`manual_locked`) به هیچ عنوان تغییر قیمت نمی‌دهند.
- [ ] Pending : **`legacy_mirror` Product Unchanged Unless Intended:** بررسی اینکه محصولات فاقد وزن مشخص (`legacy_mirror`) قیمت سایت قدیمی را آینه کرده و توسط محاسبه‌گر وزنی تغییر نمی‌کنند.
- [ ] Pending : **Telegram Daily Rate Preview:** بررسی صحت محاسبه پیش‌نمایش قیمت روز نقره در تلگرام و ارائه لیست ۲۰ تغییر بزرگ قیمت پیش از کلیک روی دکمه اعمال.

---

## 4. UI/UX, Static Pages & Mobile Scenarios (`سناریوهای رابط کاربری و صفحات استاتیک`)

- [ ] Pending : **Image Loading / Performance Sanity Check:** بررسی سرعت بارگذاری تصاویر گالری محصول (مربع ۱:۱ WebP زیر ۲۰۰ کیلوبایت) و وضوح درخشش نقره روی پس‌زمینه مشکی مات.
- [ ] Pending : **Persian Static Pages Accessibility:** بررسی در دسترس بودن و سلامت لینک‌های ۱۱ صفحه استاتیک فارسی (درباره ما، تماس با ما، حریم خصوصی، قوانین خرید، سوالات متداول، راهنمای سایز انگشتر، راهنمای نگهداری، اصالت نقره ۹۲۵ و...).
- [ ] Pending : **Mobile Responsiveness Check:** تست صفحه اصلی، صفحه محصول و سبد خرید روی ابعاد نمایشگرهای موبایل (`390x844 px` و `360x780 px`) و صحت چیدمان RTL.

---

## 5. Security, API & Infrastructure Scenarios (`سناریوهای امنیت، وب‌سرویس و بک‌آپ`)

- [ ] Pending : **Backup Creation Test:** اجرای دستی بک‌آپ در افزونه UpdraftPlus و تأیید تولید فایل‌های پشتیبان دیتابیس و فایل سیستم.
- [ ] Pending : **Restore Drill Placeholder:** تست آزمایشی بازیابی بک‌آپ روی محیط استیجینگ (`Status: TBD / PENDING` — نیازمند تأیید فرآیند روی سرور میزبان).
- [ ] Pending : **REST API Authentication Test:** ارسال درخواست احراز هویت‌شده به `/wp-json/wc/v3/products` و تأیید بازگشت کد `HTTP 200 OK`.
- [ ] Pending : **Legacy API Connectivity Test Placeholder:** تست ارتباط خواندنی (`GET`) اسکریپت `Agent-LegacySync` با پنل مدیریت سایت قدیمی `noghrehmashhad.ir` از روی سرور داخلی ایران (`Status: TBD / PENDING` - سرور میزبان `Sonic 30`).
