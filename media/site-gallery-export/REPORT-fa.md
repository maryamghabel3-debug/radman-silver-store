# اصلاحیهٔ مالک — رد ۱۰ تصویر Step3 (۲۰۲۶-۰۹-۱۷)

**شاخه:** `ai-social-luxury-20260911` · **main دست‌نخورده** · **سایت دست‌نخورده**

تأیید کلی قبلی دربارهٔ ۱۰ تصویر Step3 **لغو شد**؛ این تصاویر به دلیل تغییر حکاکی و/یا رکاب و در نتیجه
جابه‌جایی هویت انگشتر مردودند.

## ردیف‌های مردود (۱۰)
`61-B · 65-A · 65-B · 69-A · 69-B · 73-A · 73-B · 77-A · 77-B · 256-A`

برای هر ۱۰ ردیف در manifest ثبت شد:
```
qa_status=FAILED_QA
owner_approval_status=OWNER_REJECTED
deployment_action=DO_NOT_DEPLOY
notes=IDENTITY_DRIFT: engraving and/or shank changed
```

## وضعیت نهایی
| مورد | مقدار |
|---|---|
| `REJECTED_STEP3` | ۱۰ |
| `STEP3_NEW_DEPLOYABLE` | ۰ |
| فایل‌های Step3 صادرشده | ۰ |
| آرشیو `step3-site-export.zip` | **NOT_FOR_DEPLOY** (فایل حذف/بازتولید نشد) |
| ۴۲ تصویر Step3 قبلاً متصل‌شده | دست‌نخورده (SKIP) |
| ۳ فایل مبهم | دست‌نخورده (AMBIGUOUS_IDENTITY، صادر نشده) |
| ۴۷ تصویر Step4 | **بدون هیچ تغییر** — تأیید و فایل‌ها سرجای خود (فایل زیپ Step4 هم بازتولید نشد) |

## ترتیب گالری
تصویر اصلی/فعلی حفظ می‌شود → سپس ۴۷ تصویر تأییدشدهٔ Step4. هیچ فایل Step3 قابل انتشار نیست.

## دست‌نزدن‌ها (طبق دستور)
هیچ تصویری حذف، overwrite، re-encode یا دوباره تولید نشد · هیچ زیپ جدیدی ساخته نشد · هیچ اتصالی به سایت
و main انجام نشد · این اصلاحیه فقط از طریق `manifest.json`، `manifest.csv` و همین گزارش اعمال شد
(هیچ فایل نشانه‌گذاری جداگانه‌ای اضافه نشد).
