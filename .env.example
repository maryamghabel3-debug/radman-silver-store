# ==============================================================================
# RADMAN SILVER STORE — ENVIRONMENT CONFIGURATION TEMPLATE (.env.example)
# ==============================================================================
# INSTRUCTIONS: Copy this file to .env and populate with genuine production credentials.
# SECURITY WARNING: NEVER commit .env to GitHub or share in public chat logs.
# ==============================================================================

# --- 1. WORDPRESS DATABASE & ENVIRONMENT CREDENTIALS ---
DB_NAME=radman_wp
DB_USER=radman_wp_user
DB_PASSWORD=[PASSWORD]
DB_HOST=localhost

# --- 2. WORDPRESS & WOOCOMMERCE REST API CREDENTIALS ---
WP_SITE_URL=https://radmansilver.ir
WC_CONSUMER_KEY=ck_your_woocommerce_consumer_key_here
WC_CONSUMER_SECRET=cs_your_woocommerce_consumer_secret_here

# --- 3. LEGACY STORE API (noghrehmashhad.ir SOURCE OF TRUTH) ---
LEGACY_API_BASE_URL=https://noghrehmashhad.ir
LEGACY_API_KEY=[TO_BE_PROVIDED]
LEGACY_API_SECRET=your_api_secret_if_needed

# --- 4. TELEGRAM MANAGEMENT BOT (HITL APPROVAL GATEWAY) ---
TELEGRAM_BOT_TOKEN=[TO_BE_PROVIDED]
TELEGRAM_OWNER_CHAT_ID=[TO_BE_PROVIDED]
TELEGRAM_ADMIN_IDS=100000001,100000002

# --- 5. ZARINPAL PAYMENT GATEWAY (IRANIAN SHETAB DEBIT CARD GATEWAY) ---
ZARINPAL_MERCHANT_ID=[TO_BE_PROVIDED]
ZARINPAL_SANDBOX_MODE=false

# --- 6. KAVENEGAR SMS GATEWAY (OTP & ORDER TRANSACTIONAL ALERTS) ---
KAVENEGAR_API_KEY=[TO_BE_PROVIDED]
KAVENEGAR_SENDER_NUMBER=1000xxxxxx

# --- 7. PRICING RULES & ROUNDING DEFAULTS ---
SILVER_DEFAULT_DAILY_RATE_TOMAN=85000
DEFAULT_ROUNDING_STEP_TOMAN=10000

# --- 8. APPLICATION SETTINGS ---
APP_ENV=production
LOG_LEVEL=INFO
STAGING_DB_PATH=./legacy_sync_map.db
